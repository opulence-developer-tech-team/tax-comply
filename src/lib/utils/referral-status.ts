/**
 * Referral Status Enum
 * Used for referral earnings tracking
 */

export enum ReferralStatus {
  Pending = "pending",
  Available = "available",
  Withdrawn = "withdrawn",
  Cancelled = "cancelled"
}

