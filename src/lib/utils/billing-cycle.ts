/**
 * Client-Safe Billing Cycle Enum
 * 
 * This enum matches the server-side BillingCycle enum
 * and can be safely used in client components.
 */

export enum BillingCycle {
  Monthly = "monthly",
  Yearly = "yearly"
}

