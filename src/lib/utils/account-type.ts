/**
 * Client-Safe Account Type Enum
 * 
 * This enum matches the server-side AccountType enum
 * and can be safely used in client components.
 */

export enum AccountType {
  Company = "company",
  Individual = "individual",
  Business = "business"
}

