/**
 * Employee Sort Field Enum
 * Used for sorting employees by different fields
 * CRITICAL: Use enum, not string literals
 */

export enum EmployeeSortField {
  CreatedAt = "createdAt",
  FirstName = "firstName",
  LastName = "lastName",
  EmployeeId = "employeeId",
  Email = "email",
  DateOfEmployment = "dateOfEmployment",
  Salary = "salary",
}

